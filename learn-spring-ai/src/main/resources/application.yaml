spring:
  application:
    name: learn-spring-ai

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: qwen3:0.6b

    openai:
      api-key:
      chat:
        options:
          model: gpt-4
          temperture: 0.9


logging:
  level:
    org.springframework.ai:
      chat.client: DEBUG
